<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:chaturbate="clr-namespace:StormLib.Services.Chaturbate;assembly=StormLib">

    <DataTemplate DataType="{x:Type chaturbate:ChaturbateStream}">
        <Grid Style="{StaticResource gridStyle}">
            <ContentPresenter ContentTemplate="{StaticResource BaseStreamTemplate}"
                              Style="{StaticResource contentPresenterStyle}" />
        </Grid>
    </DataTemplate>

</ResourceDictionary>
